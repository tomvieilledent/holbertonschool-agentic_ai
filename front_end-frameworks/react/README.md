# Agentic AI — React

A landing page rebuilt as a pixel-for-pixel copy as part of the Holberton School curriculum, to learn the basics of React.

Stack: React + Vite, Tailwind CSS, Lucide React.

Run locally: `npm install` then `npm run dev` (production build: `npm run build`).
